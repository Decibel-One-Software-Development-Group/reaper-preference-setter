<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>SiRPS</title>
    <link>https://github.com/Decibel-One-Software-Development-Group/reaper-preference-setter</link>
    <description>SiRPS — REAPER preferences and DiGiCo session track lists (Decibel One)</description>
    <language>en</language>
    <item>
      <title>SiRPS 3.0.0</title>
      <link>https://github.com/Decibel-One-Software-Development-Group/reaper-preference-setter/releases/tag/v3.0.0</link>
      <sparkle:version>3.0.0</sparkle:version>
      <sparkle:shortVersionString>3.0.0</sparkle:shortVersionString>
      <description><![CDATA[
<p>Now <strong>SiRPS</strong>, on the Decibel One pipeline: the suite icon, hand-written release notes, and a landing page of its own.</p>
<h3>Records over MADI, not just a SoundGrid card</h3>
<p>A session that records to MADI was refused outright — "The console doesn't appear to have a Reaper/SoundGrid card configured" — even when the Copy Audio preset was perfectly good. On MADI the record ports carry the console's device prefix, <code>1:MADI 1</code>, and the rule for a record port required the name to start with a letter. Nothing matched, no destination resolved to a Reaper input, and the whole session failed. MADI now reads the same as a SoundGrid card.</p>
<h3>You choose the ports you record to</h3>
<p>Copy Audio names its own destination, so the app finds that port by itself. It could never find a <strong>second</strong> stream — a program mix, press feeds, a matrix patched straight to an output — because nothing in Copy Audio refers to them. Those channels were simply missing from the CSV with nothing to say so.</p>
<p>The DiGiCo tab now lists the record outputs it can see, with what is on each one, and you tick the ports you record to. The track count updates as you tick. Everything patched to any ticked port lands in one CSV.</p>
<p>Ports are laid out in console order, each taking its full width, which is how the interface hands them to Reaper: MADI 1 holds inputs 1-64 even when only 1-62 are patched, and MADI 2 starts at 65. Unpatched channels come through as blank rows, and that is what keeps the numbering honest.</p>
<p>The racks Copy Audio <strong>reads from</strong> are kept off the list on purpose. Reverb and aux returns get patched back out to those same Dante ports, so offering them would invite effects returns into the track list.</p>
<p>Record cards are unaffected: two that number straight through — <code>Trks 1-64</code> then <code>Tracks 65-128</code> — still land where their own numbering says.</p>
<h3>More tracks arrive with their real name</h3>
<ul>
<li>Anything the Copy Audio preset copies is now an input by definition. Channels patched from ports an engineer had renamed outright — <code>P.A Trk 1 L</code>, <code>KCmp 1</code>, <code>Orch Sum L/R</code> — resolved no route at all and fell back to a raw port name. On one 130-track session that moved 105 channels to 124.</li>
<li>Stereo channels pair by port order instead of by bumping a trailing number in the port name, so a channel on a named stagebox socket (<code>S4-4 K1 L</code>) gets its <code>.R</code> row rather than leaving a raw port name on the next track.</li>
<li>Where several channels legitimately claim one input — a tech-listen channel fed from the same playback as the primary — the lowest-numbered channel wins it, that being the primary by desk convention.</li>
<li>Sessions from older Quantum software (<code>vM</code>) parse, and a rig whose record card isn't called "Waves" is no longer assumed away.</li>
</ul>
<h3>Backup mics and mono pairs arrive named</h3>
<p>A DiGiCo channel has two inputs, main and alt, and a principal's backup receiver sits on the alt. Only the main was ever read, so a backup copied to its own Reaper track came through as a bare port name — <code>R-Dnt 5</code> where the channel above it said <code>Oliver</code>. Alt inputs are read now and named after their channel: <code>Oliver ALT</code>. Where a port is one channel's main and another's alt the main wins it, and where a tech-listen channel carries the same alt as the primary, the lower-numbered channel wins — the same rule main inputs already followed.</p>
<p>Two adjacent mono tracks called <code>Program L</code> and <code>Program R</code> are now written <code>Program.L</code> and <code>Program.R</code>, so Reaper imports them as one stereo track rather than two unrelated monos. Only adjacent pairs are joined, because those are the only ones Reaper can pair; a name that merely ends in R, like <code>Band Rev 1 L/R</code>, is left alone.</p>
<p>On the session this was built against, those two changes together took the CSV from five raw port names and five unpaired mono pairs to every track named and every pair joined.</p>
<h3>Looks like the suite, ships like the suite</h3>
<p>The element-tile icon and the SiRPS name. Signed, notarized and stapled as before, on both Apple Silicon and Intel from one universal download.</p>
<p>There is a page of its own now, and the app has an <strong>About</strong> box and <strong>Check for Updates…</strong> — in the application menu on macOS, under Help on Windows. It reads the same Sparkle appcast the rest of the suite publishes, tells you when a newer build exists, and opens the download. Installing stays a drag to Applications: replacing a running app from inside itself is where updaters go wrong, and this is a tool you open a few times a show.</p>
<p>Release notes are written by hand now, and a tag without them fails the build rather than shipping a release nobody can read.</p>
<hr/>
<h3>Version history</h3>
<ul>
<li><strong>3.0.0</strong> — Now SiRPS, with the suite icon. Records over MADI as well as a SoundGrid card, and you pick which ports you record to, so a second stream carrying outputs patched straight to it — program and press feeds — comes through in the same CSV. More channels resolve to their real name: Copy Audio sources count as inputs, stereo pairs by port order, the lowest-numbered channel wins a shared input, alt inputs arrive named (Oliver ALT), adjacent mono L/R pairs are dotted so Reaper makes them stereo, and older 'vM' sessions parse.</li>
</ul>
<p>Releases before 3.0.0 predate this ledger; their notes are on the Releases page.</p>
      ]]></description>
      <pubDate>Sun, 06 Sep 2026 00:36:11 +0000</pubDate>
      <enclosure
        url="https://decibel-one-software-development-group.github.io/reaper-preference-setter/SiRPS-3.0.0-universal.dmg"
        sparkle:version="3.0.0"
        sparkle:shortVersionString="3.0.0"
        length="19449741"
        type="application/octet-stream"
        sparkle:edSignature="qfSaNuFmwKosMZkOQj+LuG7x7K7hInxjm9hJUczowdJY8njNGknBQaCHbHfgdoyfWdpwOrlWnJhJKk6YqyDWDg=="/>
      <sparkle:minimumSystemVersion>11.0</sparkle:minimumSystemVersion>
    </item>
    <item>
      <title>SiRPS 3.1.0</title>
      <link>https://github.com/Decibel-One-Software-Development-Group/reaper-preference-setter/releases/tag/v3.1.0</link>
      <sparkle:version>3.1.0</sparkle:version>
      <sparkle:shortVersionString>3.1.0</sparkle:shortVersionString>
      <description><![CDATA[
<h3>REAPER's save dialog opens on a name you chose</h3>
<p>Saving a new project meant typing the name at the dialog every time, so a run of shows came out named by whatever you managed at the moment of saving.</p>
<p>The Preferences tab now has a <strong>New project name</strong> row: a <strong>prefix</strong> for the production and a <strong>date</strong> format to pick from. Type <code>MHET</code>, choose <code>2026-09-17_1430</code>, and REAPER's Save New Project dialog opens on <code>MHET_2026-09-17_1430</code>. The resolved name is shown as you set it, so it is confirmed before you apply rather than at the next save.</p>
<p>This is REAPER's own feature — the save-as wildcard pattern, with REAPER's own wildcards, and REAPER resolves them. The app composes the pattern and reads it back, which means the setting lives in REAPER where it belongs and keeps working whether or not you open this app again. A pattern you wrote by hand in REAPER is read back intact rather than rewritten.</p>
<p>Five date formats, ISO first because it is the only one that sorts into date order in Finder once a season has filled a folder. <code>(no date)</code> gives a plain prefix. Leave both empty and REAPER's own default is left alone.</p>
<h3>Reports its real version</h3>
<p>The 3.0.0 bundle told Finder it was version <code>0.0.0</code>, with <code>CFBundleVersion</code> empty altogether. Pinning the version reached the app's own About box and update check but not the bundle PyInstaller writes around it. Both keys are stamped from the tag now, before signing.</p>
<hr/>
<h3>Version history</h3>
<ul>
<li><strong>3.1.0</strong> — REAPER's Save New Project dialog opens on a name you chose: a production prefix plus a date format, written to REAPER's own save-as wildcard pattern and previewed as you set it. The bundle reports its real version to Finder instead of 0.0.0.</li>
</ul>
<ul>
<li><strong>3.0.0</strong> — Now SiRPS, with the suite icon. Records over MADI as well as a SoundGrid card, and you pick which ports you record to, so a second stream carrying outputs patched straight to it — program and press feeds — comes through in the same CSV. More channels resolve to their real name: Copy Audio sources count as inputs, stereo pairs by port order, the lowest-numbered channel wins a shared input, alt inputs arrive named (Oliver ALT), adjacent mono L/R pairs are dotted so Reaper makes them stereo, and older 'vM' sessions parse.</li>
</ul>
<p>Releases before 3.0.0 predate this ledger; their notes are on the Releases page.</p>
      ]]></description>
      <pubDate>Thu, 17 Sep 2026 18:27:01 +0000</pubDate>
      <enclosure
        url="https://decibel-one-software-development-group.github.io/reaper-preference-setter/SiRPS-3.1.0-universal.dmg"
        sparkle:version="3.1.0"
        sparkle:shortVersionString="3.1.0"
        length="19459713"
        type="application/octet-stream"
        sparkle:edSignature="Y4QRH5syP9aw6F6StSVa8TQOaxbTjAmll5vf7NLmWawWjhGo4HyNdbLC8zM254FPT9GeDb8WAqNiB8ACvswJDg=="/>
      <sparkle:minimumSystemVersion>11.0</sparkle:minimumSystemVersion>
    </item>
    <item>
      <title>SiRPS 3.2.0</title>
      <link>https://github.com/Decibel-One-Software-Development-Group/reaper-preference-setter/releases/tag/v3.2.0</link>
      <sparkle:version>3.2.0</sparkle:version>
      <sparkle:shortVersionString>3.2.0</sparkle:shortVersionString>
      <description><![CDATA[
<h3>Four preferences that never actually worked</h3>
<p>Every setting in the Preferences tab wrote a key and reported success. Three of them were writing the wrong key, so REAPER was never told what you asked for — and one of the three set the opposite of what its checkbox promised.</p>
<p><strong>Peak files.</strong> The <code>peaks/</code> subfolder option wrote <code>peakcachegenmode</code>, which controls only *when* peaks are generated — on import, and on project load. Peak *location* is <code>altpeaks &4</code>. Worse, <code>peakcachegenmode</code> defaults to <code>3</code>, and the code ORed bit 1 into it: <code>3 | 1</code> is <code>3</code>, so the write was a no-op even on its own terms. This is the one that left <code>.reapeaks</code> sitting beside the audio.</p>
<p><strong>Startup behaviour.</strong> <code>loadlastproj</code> is an enum, not a bitfield — 19 is "new project", 16 is "last active project". The code masked it (<code>& ~1 & ~2</code>), which turns 19 into 16. Ticking "open a new project on startup" set REAPER to reopen the last project instead.</p>
<p><strong>Prompt to save on new project.</strong> Wrote <code>saveopts &1</code>, which is "when overwriting a project file, rename the old one to .rpp-bak". The real key is <code>newprojdo &1</code>. An unrelated setting was toggled instead.</p>
<p><strong>Choosing a template</strong> also wrote <code>newprojdo=1</code>, apparently as a "use the template" flag. There is no such flag — setting <code>newprojtmpl</code> is what makes REAPER use it — and <code>newprojdo</code> is the prompt-to-save bitfield, so picking a template quietly rewrote an unrelated preference.</p>
<p>All four now write the documented key, and the media path and save path, which were already correct, are unchanged.</p>
<h3>Record-arm new tracks</h3>
<p>A new checkbox: new tracks arrive record-armed. It sets <code>deftrackrecflags &1</code>, bit 1 only — the same field carries the record-config dropdown in bits 16 to 128, which survive untouched.</p>
<h3>Checkboxes tell the truth now</h3>
<p>The four checkboxes were hardcoded to start ticked and could only ever switch a setting on, so unticking one did nothing while looking like it meant something. They now open reflecting what REAPER actually has, and toggle it either way.</p>
<h3>A New Show Project action that actually names the project</h3>
<p>REAPER applies its save-as wildcard pattern to the project it creates at launch, but not to File > New Project, so a show started mid-session arrives unnamed no matter what the pattern says. Nothing written to <code>reaper.ini</code> fixes that — it is REAPER's behaviour, not a setting.</p>
<p>So applying preferences now also installs <strong>SiRPS New Show Project</strong> into REAPER's <code>Scripts</code> folder. It reads the same pattern and save path out of REAPER's own config, shows you the resulting name to confirm or edit, then creates the dated folder, starts a new project from your template, points the media folder at <code>Audio</code> and saves into it.</p>
<p>It never touches the file dialog, which is what would have required SWS or js_ReaScriptAPI — it builds the path and saves directly. No extension needed.</p>
<p>Bind it once under Actions > Show action list and use it instead of File > New Project. It refuses to overwrite a project that already exists, and cancelling leaves everything untouched.</p>
<h3>The media path now survives a project template</h3>
<p>Setting the media folder to <code>Audio</code> had no effect for anyone using a project template. A template stores its own <code>RECORD_PATH</code>, and REAPER takes the template's over the <code>projdefrecpath</code> preference — so a template saved with an empty record path sent every recording to the top level of the project folder, no matter what the preference said.</p>
<p>It looks exactly like the media-path setting being broken, and the peaks folder compounds it: peaks nest relative to the media, so they end up at the top level too, beside the audio, rather than tucked inside it.</p>
<p>When a template is chosen and a media folder is set, the template's own record path is now set to match, with a <code>.bak-<timestamp>.RPP</code> copy kept beside it. A template that already agrees is left alone and no backup is written.</p>
<hr/>
<h3>Version history</h3>
<ul>
<li><strong>3.2.0</strong> — Three of the four Preferences settings were writing the wrong REAPER key and reporting success: peak location (the <code>peaks/</code> subfolder), startup behaviour (which set the opposite of its checkbox), and prompt-to-save. All corrected against REAPER's documented keys. New: record-arm new tracks. The checkboxes now reflect what REAPER actually has and toggle it either way. Also installs a New Show Project ReaScript, which names a mid-session project the way REAPER only does at launch, and sets a chosen template's own record path to match the media folder, which otherwise overrides it.</li>
</ul>
<ul>
<li><strong>3.1.0</strong> — REAPER's Save New Project dialog opens on a name you chose: a production prefix plus a date format, written to REAPER's own save-as wildcard pattern and previewed as you set it. The bundle reports its real version to Finder instead of 0.0.0.</li>
</ul>
<ul>
<li><strong>3.0.0</strong> — Now SiRPS, with the suite icon. Records over MADI as well as a SoundGrid card, and you pick which ports you record to, so a second stream carrying outputs patched straight to it — program and press feeds — comes through in the same CSV. More channels resolve to their real name: Copy Audio sources count as inputs, stereo pairs by port order, the lowest-numbered channel wins a shared input, alt inputs arrive named (Oliver ALT), adjacent mono L/R pairs are dotted so Reaper makes them stereo, and older 'vM' sessions parse.</li>
</ul>
<p>Releases before 3.0.0 predate this ledger; their notes are on the Releases page.</p>
      ]]></description>
      <pubDate>Fri, 18 Sep 2026 00:31:28 +0000</pubDate>
      <enclosure
        url="https://decibel-one-software-development-group.github.io/reaper-preference-setter/SiRPS-3.2.0-universal.dmg"
        sparkle:version="3.2.0"
        sparkle:shortVersionString="3.2.0"
        length="19470563"
        type="application/octet-stream"
        sparkle:edSignature="0lrRe4yCzn6ylMTq0bgscx2S7Kai1vKFpNimrFimG7Lc0zXlkt88pUR9qM+uYRNzeMx5jUZ4quF3o9VZIGGcBA=="/>
      <sparkle:minimumSystemVersion>11.0</sparkle:minimumSystemVersion>
    </item>
    <item>
      <title>SiRPS 3.2.1</title>
      <link>https://github.com/Decibel-One-Software-Development-Group/reaper-preference-setter/releases/tag/v3.2.1</link>
      <sparkle:version>3.2.1</sparkle:version>
      <sparkle:shortVersionString>3.2.1</sparkle:shortVersionString>
      <description><![CDATA[
<h3>Check for Updates works</h3>
<p>In every build since it was added, <strong>Check for Updates…</strong> reported "Couldn't reach the update feed" with <code>CERTIFICATE_VERIFY_FAILED</code>. The feed was fine; the app had nothing to check its certificate against.</p>
<p>HTTPS needs a bundle of trusted certificate authorities. A packaged app's OpenSSL looks for that file at a path baked in from the machine it was *built* on — a Python installation that does not exist on yours — so every secure request failed. It never showed during development, because Apple's own Python trusts the macOS keychain regardless of any file.</p>
<p>The app now carries its own certificate bundle and uses it for the update check. Verification is still fully on; nothing about the check was loosened.</p>
<p>One consequence worth knowing: because the check was broken in 3.0.0 through 3.2.0, those versions cannot tell you about this one. If you are on any of them, download 3.2.1 once from the SiRPS page — after that, updates will find you.</p>
<hr/>
<h3>Version history</h3>
<ul>
<li><strong>3.2.1</strong> — Check for Updates works. Every earlier build failed with CERTIFICATE_VERIFY_FAILED because the packaged app had no certificate bundle to verify against; it now ships its own. 3.0.0–3.2.0 cannot announce this release, so download it once by hand.</li>
</ul>
<ul>
<li><strong>3.2.0</strong> — Three of the four Preferences settings were writing the wrong REAPER key and reporting success: peak location (the <code>peaks/</code> subfolder), startup behaviour (which set the opposite of its checkbox), and prompt-to-save. All corrected against REAPER's documented keys. New: record-arm new tracks. The checkboxes now reflect what REAPER actually has and toggle it either way. Also installs a New Show Project ReaScript, which names a mid-session project the way REAPER only does at launch, and sets a chosen template's own record path to match the media folder, which otherwise overrides it.</li>
</ul>
<ul>
<li><strong>3.1.0</strong> — REAPER's Save New Project dialog opens on a name you chose: a production prefix plus a date format, written to REAPER's own save-as wildcard pattern and previewed as you set it. The bundle reports its real version to Finder instead of 0.0.0.</li>
</ul>
<ul>
<li><strong>3.0.0</strong> — Now SiRPS, with the suite icon. Records over MADI as well as a SoundGrid card, and you pick which ports you record to, so a second stream carrying outputs patched straight to it — program and press feeds — comes through in the same CSV. More channels resolve to their real name: Copy Audio sources count as inputs, stereo pairs by port order, the lowest-numbered channel wins a shared input, alt inputs arrive named (Oliver ALT), adjacent mono L/R pairs are dotted so Reaper makes them stereo, and older 'vM' sessions parse.</li>
</ul>
<p>Releases before 3.0.0 predate this ledger; their notes are on the Releases page.</p>
      ]]></description>
      <pubDate>Fri, 18 Sep 2026 13:51:09 +0000</pubDate>
      <enclosure
        url="https://decibel-one-software-development-group.github.io/reaper-preference-setter/SiRPS-3.2.1-universal.dmg"
        sparkle:version="3.2.1"
        sparkle:shortVersionString="3.2.1"
        length="19594215"
        type="application/octet-stream"
        sparkle:edSignature="L8poRNvv8s8l63RzW0zFTdpyMqFSz4B/qJvQaibcFipTQQVKA38cM0+DMkL6DTgc6RzZOgDbcbwoy5J67i89BA=="/>
      <sparkle:minimumSystemVersion>11.0</sparkle:minimumSystemVersion>
    </item>
    <item>
      <title>SiRPS 3.2.2</title>
      <link>https://github.com/Decibel-One-Software-Development-Group/reaper-preference-setter/releases/tag/v3.2.2</link>
      <sparkle:version>3.2.2</sparkle:version>
      <sparkle:shortVersionString>3.2.2</sparkle:shortVersionString>
      <description><![CDATA[
<h3>Apply sets REAPER up properly on a fresh machine</h3>
<p>On a machine that had never been set up, pressing Apply left REAPER reopening the last project at startup instead of starting a new one.</p>
<p>3.2.0 changed the Preferences checkboxes to open showing whatever REAPER already had, rather than the recommended setup. On a fresh install REAPER's own default is "reopen the last project", so the startup box opened unticked — and because Apply now sets each option both ways, Apply wrote that default straight back. The same went for prompt-to-save and the peaks subfolder.</p>
<p>They open ticked again, because setting REAPER up the standard way is what the app is for. Unticking one still genuinely turns it off. <strong>Record-arm new tracks</strong> is the exception: it's a personal choice rather than part of the standard setup, so it still opens on whatever REAPER has — off on a fresh install, and left on for anyone who turned it on.</p>
<h3>Audio lands in Audio/ whatever template REAPER is using</h3>
<p>On the same machine the peaks nested but the audio didn't, recording straight into the project folder.</p>
<p>A project template carries its own media path, and it overrides the preference. Apply sets the template's media path to match — but only for the template picked in the dropdown, and the dropdown recognised a template only if it lived in <code>ProjectTemplates</code> and its name matched loosely. A template REAPER was already using that the dropdown didn't recognise showed as "(none)", stayed in use, and was never touched, so its empty media path won.</p>
<p>Apply now reads back which template REAPER will actually open and aligns that one, wherever it lives. The dropdown shows it too, instead of "(none)", and matches it exactly — "Show.RPP" no longer mistakes itself for "My Show.RPP".</p>
<h3>Apply double-checks everything, every time</h3>
<p>Apply used to skip a setting when it already looked right, and then report what it had meant to do. Both of those trusted a single read of <code>reaper.ini</code> that could itself be wrong. Now:</p>
<ul>
<li><strong>Every setting is written on every Apply</strong>, whether or not it looked correct already. Nothing is skipped on the strength of a read.</li>
<li><strong>The template is aligned on every Apply</strong> — whichever one REAPER will open, however it came to be set.</li>
<li><strong>Everything is read back off disk afterwards</strong>, and the report lists each setting with ✓ or ✗ against what is actually there, not what was intended. If anything did not hold, the dialog says so and says why.</li>
<li><strong>A setting that appears twice</strong> in <code>reaper.ini</code> is collapsed to the one value written. Before, the first copy was updated and read back as proof, while REAPER could be reading the other.</li>
<li><strong>Apply refuses while REAPER is running.</strong> REAPER keeps its preferences in memory and writes them all back when it quits, which silently undoes anything written meanwhile — the most likely reason a setting "doesn't stick". This was only checked when SiRPS opened, so launching REAPER afterwards went unnoticed.</li>
</ul>
<hr/>
<h3>Version history</h3>
<ul>
<li><strong>3.2.2</strong> — On a fresh machine, Apply now sets REAPER up the standard way instead of writing its defaults back: startup opens a new project, and the checkboxes open on the recommended setup. Audio lands in Audio/ whatever template REAPER is using, including one outside ProjectTemplates. Apply now writes every setting and the template every time, reads them all back off disk, reports ✓/✗ per setting, and refuses while REAPER is running.</li>
</ul>
<ul>
<li><strong>3.2.1</strong> — Check for Updates works. Every earlier build failed with CERTIFICATE_VERIFY_FAILED because the packaged app had no certificate bundle to verify against; it now ships its own. 3.0.0–3.2.0 cannot announce this release, so download it once by hand.</li>
</ul>
<ul>
<li><strong>3.2.0</strong> — Three of the four Preferences settings were writing the wrong REAPER key and reporting success: peak location (the <code>peaks/</code> subfolder), startup behaviour (which set the opposite of its checkbox), and prompt-to-save. All corrected against REAPER's documented keys. New: record-arm new tracks. The checkboxes now reflect what REAPER actually has and toggle it either way. Also installs a New Show Project ReaScript, which names a mid-session project the way REAPER only does at launch, and sets a chosen template's own record path to match the media folder, which otherwise overrides it.</li>
</ul>
<ul>
<li><strong>3.1.0</strong> — REAPER's Save New Project dialog opens on a name you chose: a production prefix plus a date format, written to REAPER's own save-as wildcard pattern and previewed as you set it. The bundle reports its real version to Finder instead of 0.0.0.</li>
</ul>
<ul>
<li><strong>3.0.0</strong> — Now SiRPS, with the suite icon. Records over MADI as well as a SoundGrid card, and you pick which ports you record to, so a second stream carrying outputs patched straight to it — program and press feeds — comes through in the same CSV. More channels resolve to their real name: Copy Audio sources count as inputs, stereo pairs by port order, the lowest-numbered channel wins a shared input, alt inputs arrive named (Oliver ALT), adjacent mono L/R pairs are dotted so Reaper makes them stereo, and older 'vM' sessions parse.</li>
</ul>
<p>Releases before 3.0.0 predate this ledger; their notes are on the Releases page.</p>
      ]]></description>
      <pubDate>Fri, 18 Sep 2026 14:13:03 +0000</pubDate>
      <enclosure
        url="https://decibel-one-software-development-group.github.io/reaper-preference-setter/SiRPS-3.2.2-universal.dmg"
        sparkle:version="3.2.2"
        sparkle:shortVersionString="3.2.2"
        length="19597436"
        type="application/octet-stream"
        sparkle:edSignature="AlRaYr7btOXSW7hmxtoV1AhISnuYiR6NrQiK9J8antd0eDRJFr9bIltOWP3B2G03GkAniXi+H5dYlp12vSM2AQ=="/>
      <sparkle:minimumSystemVersion>11.0</sparkle:minimumSystemVersion>
    </item>
  </channel>
</rss>
